# 0.2.1b Implementation Plan: Ship Scale and MVP Interior

## 1. Goal

Build the first MVP-quality ship interior that physically and visually belongs inside the current ShuttleA exterior.

This phase is no longer a temporary greybox cleanup. The target is an immersive, high-quality prototype interior for a polished video game ship:

- plausible scale relative to the player,
- interior geometry that matches the exterior model,
- cockpit/cabin layout that feels intentional,
- transparent cockpit/window treatment where practical,
- a more believable pilot chair,
- better floor, wall, ceiling, panel, and lighting detail,
- materials and textures that move beyond placeholders,
- preserved enter/sit/fly/land/stand/exit gameplay.

This is still an MVP prototype, not final production art. It should establish the quality bar and workflow for future ship interiors.

## 2. Achievability Assessment

This is achievable now as an MVP vertical slice if we make the right tradeoffs.

Achievable in this phase:

- A cohesive cockpit/cabin interior that fits the exterior better.
- Substantially better geometry than the current rectangular shell.
- Custom or kitbashed pilot chair.
- Window/cockpit glass treatment that visually connects inside and outside.
- Better materials, lighting, and readable controls/panels.
- Correct marker placement and preserved gameplay.

Not realistic in this phase:

- Final AAA-quality authored ship interior.
- Fully animated hatch and production-grade door mechanism.
- Fully interactive cockpit instrumentation.
- Complete multi-ship interior framework.
- Perfect exterior/interior continuity for every model detail.
- Final collision for the full exterior hull.

The right target is: **MVP-quality, visibly intentional, and good enough that 0.2.1c can fit exterior collision around a stable ship scale and interior layout.**

## 3. Inputs From 0.2.1a

The spike showed:

- ShuttleA is a good visual direction.
- Standing on a landed ship is stable enough to proceed.
- Primitive collision works for broad physical masses.
- Swept wings and tail likely need convex/collision-only meshes later.
- The cockpit/canopy and current interior feel too small relative to the player.
- The current interior shell clips through or conflicts with the exterior visual model.
- Hatch entry, sitting, and ship flight remained stable during spike tests.

0.2.1b should solve the scale/interior mismatch before 0.2.1c fits exterior collision.

## 3.1 Implementation Status

Status: implementation pass substantially complete, pending host Godot validation and visual screenshot/manual review.

Implemented:

- Applied a modest ShuttleA visual scale increase by updating `OpenGameArtShuttleVisual.tscn`.
- Replaced the rectangular ship interior with a compact cockpit/cabin layout.
- Added shaped wall panels, floor plating, structural ribs, ceiling spine, cockpit console, screen accents, hatch frames, and a multi-part pilot chair.
- Added interior and exterior cockpit glass treatment.
- Added new interior materials for dark panels, rubber surfaces, glass, emissive screens, and warning accents.
- Realigned hatch positions, interior spawn, seat anchor, seat exit, ship camera distance, and interior volume.
- Preserved the current script-level hatch, seat, flight, stand, and exit interaction flow.

Pending review:

- Run the repo-side static preflight:

```bash
scripts/preflight-0.2.1b-static.sh
```

- Run the full host review path:

```bash
scripts/review-0.2.1b-host.sh
```

- After reviewing screenshots and completing the checklist, run the closeout audit:

```bash
scripts/closeout-0.2.1b.sh
```

- Run the host Godot validation suite, or the focused 0.2.1b validation script:

```bash
scripts/validate-0.2.1b.sh
```

- Generate and review automated ship alignment screenshots.
- Complete the visual/manual review checklist:

```text
reports/ship_alignment_review_checklist.md
```

- Inspect scale, hatch entry, seat alignment, cockpit readability, glass visibility, and flight camera framing.
- Tune any geometry/marker positions based on manual review.

## 3.2 Alignment Feedback Loop

The first implementation pass showed that hand-authored scene edits are not enough for the quality target. 0.2.1b now includes an automated static alignment report so future iterations are driven by mesh-derived evidence.

Run:

```bash
scripts/ship_alignment_report.sh
```

Output:

```text
reports/ship_alignment_report.md
```

The report currently checks:

- transformed ShuttleA visual mesh bounds,
- slice-by-slice hull width and height along the ship length,
- a heuristic forward upper cockpit/canopy mesh slice,
- heuristic cabin, floor/walk-path, and rear hatch fit targets,
- ship collision shape bounds,
- interior/detail mesh bounds,
- authored interior OBJ asset presence and scene references,
- required measured fit-comparison inputs,
- glass/canopy mesh positions relative to the cockpit candidate,
- collision and interior protrusion outside the visual model's broad AABB,
- top-projection empty-space estimates for broad rectangular bounds,
- fixed-camera capture contract consistency between the C# runner, manifest, dashboard, and overlay scene.
- visual review checklist contract consistency, including required sections and checklist item count.

For validation gating, run:

```bash
python3 tools/ship_alignment_report.py --check
```

For the full repo-side static preflight, run:

```bash
scripts/preflight-0.2.1b-static.sh
```

This runs the Python alignment tools, validates generated JSON, checks shell script syntax, and runs `git diff --check`. It does not replace host Godot/.NET validation.

For the full host-side review path, run:

```bash
scripts/review-0.2.1b-host.sh
```

This runs static preflight, C# build, Godot import, 0.2.1b Godot validation, fixed-camera screenshot capture, screenshot manifest verification, and dashboard refresh.

The host review script checks `DOTNET_BIN`/`dotnet` and `GODOT_BIN` before running the build. If those are unavailable after a reboot, run:

```bash
source .tools/env.sh
```

Then rerun the host review script.

After completing screenshot and gameplay review, run:

```bash
scripts/closeout-0.2.1b.sh
```

This refreshes alignment artifacts and fails until the screenshot manifest is complete, the review checklist is fully checked, and the completion audit passes.

The check mode fails if:

- required authored interior OBJ files are missing, too small to be useful, or not referenced by `Ship.tscn`,
- required fit targets or comparison nodes are missing,
- required authored interior meshes disappear from the report,
- collision or interior/detail bounds protrude outside the ShuttleA visual AABB,
- canopy alignment drifts beyond the measured cockpit/canopy target,
- floor alignment drifts beyond the measured walk-path target.

It also generates projection diagrams:

```text
reports/ship_alignment_top.svg
reports/ship_alignment_side.svg
reports/ship_alignment_front.svg
reports/ship_hull_profile.csv
reports/ship_fit_targets.json
reports/ship_fit_recommendations.md
reports/ship_fit_recommendations.json
reports/ship_alignment_dashboard.html
reports/README.md
reports/ship_alignment_captures.json
reports/ship_alignment_review_checklist.md
reports/ship_phase_0_2_1b_completion_audit.md
reports/ship_phase_0_2_1b_host_review.log
scenes/debug/generated/ShipAlignmentOverlay.tscn
```

These diagrams overlay:

- gray points: transformed ShuttleA OBJ vertices,
- red boxes: collision shapes,
- blue boxes: interior/detail meshes,
- cyan boxes: glass/canopy pieces,
- orange box: heuristic cockpit/canopy mesh slice.

This is still not sufficient for final visual judgment because AABB checks cannot prove curved/swept surface fit. The next efficient step is to add host-side screenshot capture with fixed exterior and interior debug cameras, then iterate from both screenshots and the static alignment report.

Screenshot capture has been added.

Run on the host:

```bash
scripts/capture-ship-alignment.sh
```

This command:

- regenerates `reports/ship_alignment_report.md`,
- removes the expected 0.2.1b fixed-camera PNGs from previous runs,
- launches `res://scenes/debug/ShipAlignmentCapture.tscn`,
- saves fixed-view screenshots through `ShipAlignmentCaptureRunner.cs`.

Screenshots are written into the project report folder:

```text
reports/ship_alignment_captures
```

Capture views are generated in clean and overlay variants:

- exterior front,
- rear hatch,
- exterior side,
- exterior top,
- cockpit glass close-up,
- interior entry,
- interior seat,
- interior cockpit looking back.

The clean captures are for art/scale readability. The overlay captures use the generated measurement boxes to evaluate physical alignment against the same fit targets used by the static report.

Iteration rule:

- If the static report says a collision or interior piece exceeds the visual AABB, fix that before aesthetic tuning.
- If the hull profile shows a shape is being evaluated against a broad rectangular section, add a more specific mesh slice, authored marker, or generated fit target before trusting the number.
- If the top-projection empty-space estimate is high for a fitted piece, replace the broad box with a more specific authored mesh, convex hull, or split part.
- If a future script needs the measured geometry data, read `reports/ship_fit_targets.json` instead of scraping `reports/ship_alignment_report.md`.
- If `reports/ship_fit_recommendations.md` lists an `adjust` row, use that as the next measured placement change before subjective polish.
- If `reports/ship_fit_recommendations.md` lists a `review` row, inspect fixed screenshots before applying the suggested move.
- Use `reports/ship_alignment_dashboard.html` as the primary review surface because it combines gate state, measured recommendations, projection overlays, and fixed-camera screenshots.
- Use `reports/README.md` as the index for generated reports, manual review files, screenshots, and host closeout commands.
- Use the dashboard's embedded completion audit table as the quickest summary of remaining closeout gates.
- Use `reports/ship_alignment_captures.json` to verify whether all expected fixed-camera screenshots were actually produced. The capture script fails if any of the sixteen expected clean/overlay PNGs is missing or empty after Godot runs.
- The capture script removes the expected clean/overlay PNGs before launching Godot, so the manifest verifies a fresh capture run rather than stale screenshots.
- The capture contract check ensures the C# screenshot names match the Python manifest/dashboard expectations and that the capture scene includes the generated overlay.
- Use `reports/ship_alignment_review_checklist.md` to record the required visual and gameplay review evidence before closing this phase.
- Use the dashboard's review checklist status to confirm the checklist is fully complete before marking 0.2.1b done.
- The static preflight checks that the review checklist still contains the required evidence, clean screenshot, overlay screenshot, gameplay, and deferral sections.
- Use `reports/ship_phase_0_2_1b_completion_audit.md` to confirm which completion gates are proven and which still need host evidence.
- Use `reports/ship_phase_0_2_1b_host_review.log` as the durable transcript of the host build/import/validation/capture run. The completion audit requires the log to contain `0.2.1b host review passed.`.
- Use the generated `scenes/debug/generated/ShipAlignmentOverlay.tscn` overlay in the capture scene so rendered screenshots show measured target and actual fit volumes directly over the ship.
- If the projection SVGs show a major top/side/front mismatch, fix the scene from the measured projection before screenshot polish.
- If screenshots show a mismatch that AABB checks miss, prefer adding a more specific mesh slice/marker/report check over another hand-tuned coordinate change.
- If a piece must be hand-authored for quality, move toward reusable mesh assets or generated mesh files rather than growing `Ship.tscn` indefinitely.

Remaining 0.2.1b alignment process:

1. Generate the report, SVGs, hull profile, fit-target JSON, fit recommendations, Godot overlay scene, dashboard, screenshot manifest, and screenshots.
2. Resolve any `adjust` rows in `reports/ship_fit_recommendations.md` that can be fixed from measured bounds.
3. Review `reports/ship_alignment_dashboard.html` for any `review` rows before applying subjective vertical, visibility, or readability changes.
4. For each mismatch, add or tighten a measurable report check first when practical.
5. Adjust authored OBJ assets, scene transforms, or markers.
6. Re-run the alignment gate, recommendation tool, and capture script.
7. Complete `reports/ship_alignment_review_checklist.md`.
8. Only accept the phase after both automated checks and rendered review agree that scale, glass, hatch, cockpit, and interior placement are coherent.

Current automated correction:

- The first projection/report pass showed the canopy glass was much shorter than the forward cockpit slice and offset vertically/fore-aft.
- The canopy glass and frame were expanded and recentered against the cockpit candidate.
- After correction, the static report shows substantially better canopy/cockpit overlap, though screenshot review is still required.
- A later fit-target comparison showed the floor/walk path was centered too far forward relative to the lower hull slice.
- The floor collision and floor visual plate were shifted aft and shortened slightly to better match the measured walk path.
- The interior hatch and interior spawn marker were shifted aft toward the measured rear hatch zone.
- The main cabin shell now uses project-authored OBJ meshes for the tapered floor, side wall panels, forward bulkhead, rear bulkhead, and cockpit console instead of only primitive boxes.
- The alignment report was extended to include external OBJ mesh bounds so these authored pieces remain measurable.
- The pilot chair now uses project-authored OBJ meshes for its structural frame and cushion shell, replacing the blocky base/cushion/back/headrest stack as the visible chair form.
- The alignment report skips hidden visual meshes so retired blockout pieces do not pollute projection diagrams or fit metrics.
- The exterior and interior hatch visuals now use project-authored OBJ meshes for a recessed hatch door and beveled frame instead of visible primitive boxes.
- The cockpit now uses project-authored OBJ meshes for a screen cluster and control cluster, replacing the visible primitive screen/button set.
- The cabin structural ribs now use a reusable project-authored U-frame OBJ mesh instead of separate visible box columns/crossbars.
- `assets/models/ship/interior/README.md` now records the authored asset contract for the MVP ShuttleA interior.
- The alignment report now writes `reports/ship_hull_profile.csv` so swept wings, tail height, canopy height, and tapered hull sections can be reviewed from mesh slices instead of only broad AABBs.
- The alignment report now writes `reports/ship_fit_targets.json` so measured bounds, fit targets, fit comparisons, authored assets, hull profile, and failure state can drive later automation.
- `tools/ship_fit_recommendations.py` now reads the fit-target JSON and writes `reports/ship_fit_recommendations.md` plus `reports/ship_fit_recommendations.json`.
- The measured recommendations drove a scene adjustment that moved the interior hatch and interior spawn aft toward the rear hatch target, and moved the pilot seat/seat markers forward under the cockpit/canopy region.
- `tools/ship_alignment_dashboard.py` now generates `reports/ship_alignment_dashboard.html`, a single-page review dashboard that combines measured recommendations, fit targets, projection overlays, and fixed-camera capture slots.
- `tools/ship_alignment_overlay_scene.py` now generates `scenes/debug/generated/ShipAlignmentOverlay.tscn` from the fit-target JSON. The capture scene instances this overlay so cockpit, cabin, floor, hatch, canopy, and pilot-seat fit volumes appear in rendered screenshots.
- `tools/ship_capture_manifest.py` now writes `reports/ship_alignment_captures.json` and the capture script checks that all sixteen expected fixed-camera PNGs exist and are non-empty after host Godot capture.
- `tools/ship_capture_manifest.py --clean` removes expected fixed-camera PNGs before host capture so stale screenshots cannot satisfy the manifest.
- `tools/ship_capture_contract.py` verifies that `ShipAlignmentCaptureRunner.cs`, the capture manifest, the dashboard, and `ShipAlignmentCapture.tscn` agree on the expected clean/overlay screenshot contract.
- The capture runner now records clean and overlay versions of each fixed camera view, so art readability and measured fit can be reviewed separately.
- `reports/ship_alignment_review_checklist.md` records the required visual, overlay, and gameplay review evidence before 0.2.1b can be closed.
- `tools/ship_review_checklist_contract.py` verifies that the review checklist keeps its required sections and expected checkbox count.
- The dashboard now parses `reports/ship_alignment_review_checklist.md` and reports checklist completion status.
- `tools/ship_phase_completion_audit.py` now writes `reports/ship_phase_0_2_1b_completion_audit.md`, summarizing which closeout gates are proven and which remain pending.
- The dashboard now embeds the completion audit table so pending host/log/capture/checklist gates are visible in the primary review surface.
- `reports/README.md` now indexes the generated evidence files and the required static, host review, and closeout commands.
- `scripts/review-0.2.1b-host.sh` now tees host review output to `reports/ship_phase_0_2_1b_host_review.log`, and the completion audit requires that log plus its success marker as host evidence.
- The alignment report now includes a top-projection empty-space estimate that helps identify when a rectangular collision/detail bound includes too much empty space around the actual ShuttleA mesh.
- Screenshot capture now writes to `reports/ship_alignment_captures` so rendered evidence lives next to the markdown, SVG, and CSV report artifacts.
- A dedicated `Phase021bValidation` scene now validates the revised ship interior loop, authored OBJ mesh presence, hidden legacy blockout nodes, marker layout, hatch entry, seat alignment, stand, and exit.
- `scripts/validate.sh` now runs the 0.2.1b validation after the existing Phase 1-5 checks.
- `scripts/validate-0.2.1b.sh` provides a focused host-side validation path for this phase.
- `scripts/preflight-0.2.1b-static.sh` provides a repo-side static validation path for environments that cannot execute host Godot or dotnet.
- `scripts/review-0.2.1b-host.sh` provides a one-command host review path that builds, validates, captures screenshots, verifies the capture manifest, and refreshes the dashboard.
- `scripts/review-0.2.1b-host.sh` now checks the resolved dotnet and Godot executables before starting the review run.
- `scripts/closeout-0.2.1b.sh` provides the final closeout gate after host capture and checklist review.
- `tools/ship_alignment_report.py --check` is now a failing alignment gate, and `scripts/validate-0.2.1b.sh` runs it before Godot validation.

## 4. Scope

### In Scope

- Decide and apply ship visual scale if needed.
- Establish a believable player-scale cockpit/cabin envelope.
- Redesign the ship interior geometry to match the exterior silhouette.
- Replace the box-room feeling with shaped interior geometry.
- Add MVP-quality pilot chair geometry.
- Add cockpit/window glass treatment if technically practical.
- Add interior details such as panels, structural ribs, floor plating, light strips, and cockpit surfaces.
- Improve materials and lighting for the ship interior.
- Realign hatch, seat, exit, camera, and spawn markers.
- Preserve current ship gameplay.
- Record decisions and implications for 0.2.1c.

### Out of Scope

- Full exterior collision pass.
- Final exterior hull art pass.
- Fully animated hatch.
- Fully interactive cockpit UI.
- Full multi-ship data/resource system.
- Planet object collision.
- Replacing ShuttleA.

## 5. Design Principles

### Interior Must Fit The Exterior

The interior should not feel like a rectangular room hidden inside an unrelated model.

It should:

- follow the ship's narrow cockpit/cabin proportions,
- respect the nose, canopy, hatch, and tail direction,
- avoid visible clipping through the exterior shell,
- place the pilot seat where the cockpit visually suggests it belongs,
- make windows/cockpit glass spatially meaningful.

### Quality Over Placeholder Breadth

Prefer a smaller, better interior over a larger greybox room.

Good:

- compact cockpit/cabin,
- well-shaped floor and walls,
- believable chair,
- strong lighting,
- clear hatch and seat path,
- coherent materials.

Bad:

- large rectangular room,
- flat untextured walls,
- interior protruding through exterior,
- cockpit that cannot visually fit the player,
- panels and props scattered without layout purpose.

### Author Custom Geometry Where Fit Matters

Open assets are still useful, but this interior must match the chosen ship.

Recommended:

- custom simple geometry for cockpit shell, floor, ribs, hatch frame, and chair base,
- open/CC0 textures for metal, rubber, glass, panels, and floor,
- human-reviewed external assets only for reusable cockpit props or chair/details if they fit.

## 6. Asset Strategy

### Prefer Project-Authored Geometry For

- cockpit shell,
- floor plates,
- wall/ceiling panels,
- hatch frame,
- pilot chair silhouette,
- window frame,
- structural ribs,
- marker-aligned interaction spaces.

### Open Assets May Be Used For

- textures,
- panel decals,
- knobs/buttons/screens if license and style fit,
- generic sci-fi cockpit props,
- seat detail pieces.

### Human Review Required Before Importing

- any cockpit kit,
- chair model,
- interior prop pack,
- new glass/window/cockpit asset set,
- any non-CC0 asset.

### Asset Ledger

Any imported external asset must be recorded in:

```text
assets/ASSET_LEDGER.md
```

## 7. Implementation Process

## Step 1: Scale Candidate Review

### Goal

Decide whether ShuttleA should be scaled before interior work.

### Tasks

- Use the player capsule and cockpit envelope from the spike.
- Compare current scale against likely candidates:
  - current,
  - `1.15x`,
  - `1.25x`.
- Review:
  - cockpit/canopy plausibility,
  - hatch scale,
  - seat/camera space,
  - landing footprint,
  - third-person ship camera framing.

### Decision Output

Choose:

- keep current scale,
- apply modest uniform scale,
- defer scale change with explicit reason.

### Checklist

- [x] Current scale reviewed.
- [x] At least one larger scale candidate reviewed.
- [x] Preferred scale selected.
- [x] Scale decision recorded.

## Step 2: Apply Ship Scale and Recompute Bounds

### Goal

Apply the selected ship scale consistently.

### Tasks

- Update the real visual wrapper, not only the spike scene.
- Prefer uniform scaling.
- Recompute approximate visual bounds.
- Check:
  - nose/stern direction,
  - hatch side,
  - cockpit position,
  - landing footprint,
  - camera framing.

### Checklist

- [x] Scale applied.
- [x] Visual offset adjusted if needed.
- [x] New bounds recorded.
- [ ] Ship still sits plausibly on the planet. Pending host visual/manual review.
- [ ] Ship camera still frames the ship. Pending host visual/manual review.

## Step 3: Interior Layout Design

### Goal

Define the MVP interior layout before modeling details.

### Layout Target

Recommended MVP layout:

- compact entry area near the stern/hatch,
- short walkable path to the pilot seat,
- forward cockpit with chair and visible canopy/window area,
- shaped side walls that taper with the ship,
- low ceiling/upper structure that feels like a small craft,
- enough standing room for interaction but not a large room.

### Tasks

- Sketch layout in scene using simple reference shapes.
- Decide final positions for:
  - hatch interior,
  - entry standing area,
  - pilot chair,
  - seat exit,
  - cockpit forward direction,
  - window/canopy view.
- Ensure player capsule can occupy required interaction spots.

### Checklist

- [x] Entry area defined.
- [x] Pilot seat area defined.
- [x] Walk path defined.
- [x] Window/cockpit direction defined.
- [x] Player capsule fits interaction spots.

## Step 4: Build MVP Interior Geometry

### Goal

Replace the rectangular greybox with shaped, higher-quality interior geometry.

### Geometry Pieces

Recommended components:

- floor plate with inset or segmented panels,
- sloped/tapered side wall panels,
- forward cockpit console or nose panel,
- ceiling/overhead frame,
- visible structural ribs,
- hatch frame,
- pilot chair,
- simple cockpit dashboard/panel shapes,
- window/canopy frame.

### Implementation Options

Use whichever fits the current repo best:

- Godot primitive meshes arranged carefully,
- simple custom OBJ meshes authored/generated for this project,
- human-reviewed open assets for specific details,
- hybrid of custom geometry and open textures.

### Checklist

- [x] Floor geometry upgraded.
- [x] Side wall geometry upgraded.
- [x] Cockpit/front area created.
- [x] Pilot chair upgraded.
- [x] Hatch frame created.
- [x] Interior no longer reads as a simple rectangular room.
- [x] Interior avoids obvious exterior clipping by static AABB/projection report.

## Step 5: Materials, Glass, and Lighting

### Goal

Make the interior visually readable and more polished.

### Materials

Recommended material categories:

- dark metal wall panels,
- lighter floor plating,
- rubber or fabric seat surfaces,
- emissive cockpit/panel accents,
- glass/canopy material,
- warning/accent color used sparingly.

### Lighting

Recommended:

- one soft ambient cabin light,
- small emissive strips,
- subtle cockpit glow,
- avoid black unreadable corners.

### Glass/Windows

Transparent cockpit glass is in scope if practical.

Minimum acceptable:

- visible glass/canopy material on the exterior/interior cockpit area,
- enough transparency or reflectivity to sell that cockpit and exterior are connected.

### Checklist

- [x] Interior materials assigned.
- [x] Seat material assigned.
- [x] Floor/wall material separation exists.
- [x] Emissive accents added.
- [ ] Lighting is readable. Pending screenshot/manual review.
- [x] Glass/canopy treatment attempted or explicitly deferred with reason.

## Step 6: Realign Markers and Interaction Volumes

### Goal

Preserve and improve the ship interaction loop.

### Nodes To Review

- `ExteriorHatch`
- `InteriorHatch`
- `PilotSeat`
- `Markers/InteriorSpawn`
- `Markers/ExteriorExit`
- `Markers/SeatAnchor`
- `Markers/SeatExit`
- `ShipCamera`

### Tasks

- Move hatch interaction volumes to match the revised hatch/interior geometry.
- Move interior spawn so the player enters into a valid standing area.
- Move seat anchor to align with the upgraded pilot chair.
- Move seat exit marker to a plausible standing position.
- Adjust interior volume to match the new cabin envelope.
- Adjust ship camera distance/height if scale changes substantially.

### Checklist

- [ ] Exterior hatch prompt appears in the correct place. Pending host validation/manual review.
- [ ] Entering places player inside cleanly. Pending host validation.
- [ ] Interior hatch exits player outside cleanly. Pending host validation.
- [ ] Seat prompt appears from a plausible approach. Pending host validation/manual review.
- [ ] Sitting aligns player to pilot chair. Pending host validation.
- [ ] Standing places player in a valid interior location. Pending host validation.
- [ ] Ship camera still frames the ship. Pending screenshot/manual review.

## Step 7: Preserve Gameplay Loop

### Manual Test

1. Approach ship.
2. Enter through hatch.
3. Inspect interior scale and readability.
4. Walk to pilot seat.
5. Sit.
6. Take off.
7. Fly briefly.
8. Land.
9. Stand.
10. Exit.
11. Walk around exterior and inspect clipping.

### Automated Validation

Run:

```bash
scripts/validate.sh
```

Expected:

- Phase 1-5 validation passes.
- `Phase021bValidation.tscn` passes.

### Checklist

- [ ] Enter works. Pending host validation.
- [ ] Interior context works. Pending host validation.
- [ ] Seat works. Pending host validation.
- [ ] Flight works. Pending host validation.
- [ ] Landing works. Pending host validation.
- [ ] Stand works. Pending host validation.
- [ ] Exit works. Pending host validation.
- [ ] Validation passes. Pending host validation.

## Step 8: Record Decisions For 0.2.1c

### Goal

Make exterior collision fitting depend on stable, documented ship scale/interior decisions.

Update:

```text
plans/0.2 roadmap/0.2.1a-spike-results.md
```

Add:

- chosen ship scale,
- updated visual bounds,
- interior layout decision,
- marker changes,
- remaining interior/exterior clipping risks,
- collision implications for 0.2.1c.

### Checklist

- [x] Scale decision recorded.
- [x] Interior layout recorded.
- [x] Updated bounds recorded.
- [x] Marker changes recorded.
- [x] 0.2.1c inputs recorded.

## 8. Acceptance Criteria

0.2.1b is complete when:

- The ship scale looks plausible relative to the player.
- The interior feels like a deliberate compact ship cabin/cockpit, not a rectangular placeholder.
- Interior geometry visually fits within the exterior model.
- The pilot chair is visibly more detailed and spatially aligned with the cockpit.
- Materials and lighting are improved enough to establish the quality target.
- Transparent glass/cockpit connection is present or a clear technical reason is recorded for deferral.
- The player can enter, stand, approach the chair, sit, fly, land, stand, and exit.
- Interior marker positions are coherent after scale/layout changes.
- Ship camera framing remains usable.
- `python3 tools/ship_alignment_report.py --check` passes.
- The authored interior asset manifest matches the required OBJ files referenced by `Ship.tscn`.
- The alignment report, projection SVGs, hull profile CSV, fit-target JSON, fit recommendations, Godot overlay scene, dashboard, screenshot manifest, and fixed-camera screenshots have been generated and reviewed.
- Phase 1-5 validation passes.
- 0.2.1c can fit exterior collision to the selected scale and physical layout.

## 9. Risks

### Risk: This Expands Too Much

Mitigation:

- Target one compact high-quality interior slice.
- Prioritize cockpit, seat, floor, walls, hatch frame, and lighting.
- Defer interactive instruments and final hatch animation.

### Risk: Scaling Breaks Existing Markers

Mitigation:

- Adjust hatch, seat, camera, and exit markers in the same phase.
- Run validation immediately after scale changes.

### Risk: Interior Still Cannot Plausibly Fit

Mitigation:

- Treat the ship as a compact cockpit/cabin craft.
- Consider modest visual scale increase.
- Avoid preserving the old rectangular interior dimensions.

### Risk: External Assets Do Not Match

Mitigation:

- Prefer custom geometry for layout-critical pieces.
- Use external assets only after review.
- Use open textures more freely than open geometry.

### Risk: Larger Ship Feels Worse To Fly Or Land

Mitigation:

- Keep scale changes modest.
- Do not retune flight unless necessary.
- Record flight/landing issues separately.

## 10. Files Likely To Change

Likely:

- `scenes/ship/OpenGameArtShuttleVisual.tscn`
- `scenes/ship/Ship.tscn`
- `assets/materials/ship/`
- `assets/models/ship/` if custom OBJ geometry is added
- `reports/ship_alignment_report.md`
- `reports/ship_alignment_top.svg`
- `reports/ship_alignment_side.svg`
- `reports/ship_alignment_front.svg`
- `reports/ship_hull_profile.csv`
- `reports/ship_fit_targets.json`
- `reports/ship_fit_recommendations.md`
- `reports/ship_fit_recommendations.json`
- `reports/ship_alignment_dashboard.html`
- `reports/README.md`
- `reports/ship_alignment_captures.json`
- `reports/ship_alignment_review_checklist.md`
- `reports/ship_phase_0_2_1b_completion_audit.md`
- `reports/ship_phase_0_2_1b_host_review.log`
- `scenes/debug/generated/ShipAlignmentOverlay.tscn`
- `scripts/preflight-0.2.1b-static.sh`
- `scripts/review-0.2.1b-host.sh`
- `scripts/closeout-0.2.1b.sh`
- `tools/ship_alignment_report.py`
- `tools/ship_fit_recommendations.py`
- `tools/ship_alignment_dashboard.py`
- `tools/ship_alignment_overlay_scene.py`
- `tools/ship_capture_contract.py`
- `tools/ship_capture_manifest.py`
- `tools/ship_phase_completion_audit.py`
- `tools/ship_review_checklist_contract.py`
- `plans/0.2 roadmap/0.2.1a-spike-results.md`

Possible:

- `scripts/ship/ShipController.cs`
- `scripts/ship/PilotSeat.cs`
- `scripts/ship/ShipHatch.cs`
- validation scripts if marker behavior requires updates
- `assets/ASSET_LEDGER.md` if external assets are imported

## 11. Recommended Implementation Order

1. Review scale candidates.
2. Choose and apply ship scale.
3. Define compact interior layout.
4. Build MVP interior geometry.
5. Add materials, glass, and lighting.
6. Upgrade pilot chair and cockpit details.
7. Realign markers and interaction volumes.
8. Generate alignment report, hull profile, fit-target JSON, fit recommendations, Godot overlay scene, dashboard, screenshot manifest, and projection SVGs.
9. Generate fixed-camera screenshots.
10. Tune geometry from report evidence, recommendation output, and rendered captures.
11. Run manual gameplay loop test.
12. Run validation.
13. Update spike results.
14. Proceed to `0.2.1c` planning.
